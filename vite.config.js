import { defineConfig } from "vitest/config";
export default defineConfig({
    test: {
        // Change environment to 'jsdom' or 'happy-dom'
        environment: "jsdom",
    },
});
//# sourceMappingURL=vite.config.js.map